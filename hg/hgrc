# example config (see "hg help config" for more info)

[ui]
# name and email, e.g.
# username = Jane Doe <jdoe@example.com>
username = Simon Whitaker <sw@netcetera.org>

[extensions]
# uncomment these lines to enable some popular extensions
# (see "hg help extensions" for more info)
# pager =
progress =
color =
rebase =
record =
histedit =
#smartlog = /Users/simon/src/3p/hg-experimental/hgext3rd/smartlog.py

[alias]
#sl=smartlog --template "{shortest(node)}  {author|user}  {bookmarks % '{ifeq(bookmark, current, label(\"yellow\", \" {bookmark}*\"), label(\"green\", \" {bookmark}\"))}'} {ifeq(branch, 'default', '', label(\"bold\", branch))}\n{desc|firstline}\n\n"

[smartlog]
# (remote) names to show
#repos = , remote/, default/
#names = @, master, stable
# move the top non-public stack to the second column
indentnonpublic = True
# whether to use ancestor cache (speed up on huge repos)
# useancestorcache = False


[extensions]
hgext.extdiff =
