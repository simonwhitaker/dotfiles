[extensions]
hgext.extdiff =
fbamend=
evolve=!
prompt = $HOME/src/3p/hg-prompt/prompt.py
churn =

[extdiff]
cmd.ksdiff = /usr/local/bin/ksdiff
opts.ksdiff = --changeset --wait --filelist

[ui]
# Fail to merge by default, otherwise rebases/merges with conflicts throw up
# a shit ton of Kaleidoscope or vim sessions
#merge = Kaleidoscope
#merge = :fail
username = Simon Whitaker <swhitaker@fb.com>

[merge-tools]
Kaleidoscope.executable = /usr/local/bin/ksdiff
Kaleidoscope.args = --merge --output $output --base $base -- $local $other
