[user]
    name = Simon Whitaker
    email = sw@netcetera.org
[alias]
    release-notes = !$DOTFILES/bin/git-release-notes
    is-ancestor = !$DOTFILES/bin/git-is-ancestor
    flip-flop = !$DOTFILES/bin/git-flip-flop
    publish = !git push --set-upstream origin $(git rev-parse --abbrev-ref HEAD)
    sl = !$DOTFILES/bin/git-sl
	root = rev-parse --show-toplevel
[difftool]
    prompt = false
[diff]
    tool = Kaleidoscope
[color]
    diff = auto
    status = auto
    branch = auto
    ui = true
[mergetool]
    keepBackup = true
    prompt = false
[core]
    quotepath = false
    excludesfile = /Users/simon/.gitignore
	autocrlf = input
[difftool "sourcetree"]
	cmd = /usr/local/bin/ksdiff -w \"$LOCAL\" \"$REMOTE\"
	path =
[mergetool "sourcetree"]
	cmd = /usr/local/bin/ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
	trustExitCode = true
[github]
    user = simonwhitaker
[difftool "Kaleidoscope"]
    cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[mergetool "Kaleidoscope"]
    cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
    trustExitCode = true
[merge]
    tool = Kaleidoscope
[push]
    default = simple
[fetch]
	prune = true
[includeIf "gitdir:~/src/clients/"]
    path = ~/.gitconfig.whitaker-solutions
[includeIf "gitdir:~/src/whitakersolutions/"]
    path = ~/.gitconfig.whitaker-solutions
[includeIf "gitdir:~/src/go/src/github.com/antha-lang/"]
    path = ~/.gitconfig.whitaker-solutions
[includeIf "gitdir:~/src/go/src/github.com/Synthace/"]
    path = ~/.gitconfig.whitaker-solutions
[includeIf "gitdir:~/src/go/src/repos.antha.com/"]
    path = ~/.gitconfig.whitaker-solutions
[url "git@github.com:Synthace/"]
	insteadOf = https://github.com/Synthace/
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
